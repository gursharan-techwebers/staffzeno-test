export type BaseEmailProps = {
  previewText: string;
  heading: string;
  name?: string;
  introText: string;
  url?: string;
  buttonText?: string;
  infoText?: string;
  troubleText?: string;
  changedAt?: string;
  location?: string;
};